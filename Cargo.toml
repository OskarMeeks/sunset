[package]
name = "stock_tracker"
version = "0.1.0"
edition = "2021"

[dependencies]
# Modern async runtime for 2026
tokio = { version = "1.50", features = ["full"] }
# The primary library for Yahoo Finance data
yahoo_finance_api = "4.1"
# For precise timestamps in your CSV
chrono = "0.4.44"
